# Dashboard / cloud dependencies ONLY.
#
# This is the file Streamlit Community Cloud installs, so it is deliberately
# lean — no Kafka client, no stream processor — keeping the hosted build small
# and reliable. The dashboard only reads DuckDB/MotherDuck.
#
# For the streaming pipeline (producer/processor/sink) install the superset:
#     pip install -r requirements-pipeline.txt
streamlit==1.39.0
duckdb==1.1.3
pandas==2.2.3
python-dotenv==1.0.1
